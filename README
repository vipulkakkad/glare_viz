> Make sure that NPM is installed
> Make sure debugger for Chrome is installed
> npm install -g gulp
> npm install gulp-webserver
> npm install typescript
> Run build task: tsc -w
> In new terminal: gulp
> F5